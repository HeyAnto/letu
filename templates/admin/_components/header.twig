<header class="flex justify-between items-center">
  <div class="flex items-center gap-4">
    <img class="min-profile-picture" src="{{ asset('images/profile-picture/pp-admin-default.svg') }}"
      alt="{{ app.user.email }} Profile Picture">
    <p>{{ app.user.email }}</p>
  </div>
  <div class="flex items-center gap-7">
    <a class="btn" href="{{ path('index') }}">Retour</a>
    <a class="btn btn-red" href="{{ path('app_logout') }}">Déconnexion</a>
  </div>
</header>