{% extends 'admin/admin-base.html.twig' %}

{% block main %}
<section class="flex pt-15 gap-7">
  <a class="btn" href="{{ path('admin_difficulty_index') }}">Retour à la liste</a>
  <a class="btn" href="{{ path('admin_difficulty_edit', { id: difficulty.id }) }}">Modifier la difficulté</a>
  <a class="btn btn-red delete-action" data-url="{{ path('admin_difficulty_delete', { id: difficulty.id }) }}">
    Supprimer la difficulté
  </a>
</section>

<section class="flex flex-column pt-15 gap-15">
  <h1>{{ difficulty.label }}</h1>

  <table class="table">
    <tbody>
      <tr>
        <th>ID</th>
        <td>{{ difficulty.id }}</td>
      </tr>

      <tr>
        <th>Label</th>
        <td>{{ difficulty.label }}</td>
      </tr>
    </tbody>
  </table>
</section>

<div class="modal-overlay" id="confirmModal">
  <div class="modal-content flex flex-column gap-15">
    <p>Êtes-vous sûr de vouloir supprimer cette difficulté ?</p>
    <div class="flex justify-center gap-15">
      <button class="btn btn-red" id="confirmDelete">Supprimer</button>
      <button class="btn" id="cancelDelete">Annuler</button>
    </div>
  </div>
</div>
{% endblock %}