{% extends 'admin/admin-base.html.twig' %}

{% block main %}
<section class="flex flex-column pt-15">
  <a class="btn" href="{{ path('admin_category_index') }}">Retour à la liste</a>
</section>

<section class="flex flex-column pt-15 gap-15">
  <div>
    <h1>Modifier la catégorie</h1>
    <h2 class="text-green">{{ category.label }}</h2>
  </div>

  {{ form_start(form) }}

  {{ form_widget(form) }}

  <button class="btn btn-green mt-8" type="submit">Modifier</button>

  {{ form_end(form) }}
</section>
{% endblock %}