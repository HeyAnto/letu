{% extends 'admin/admin-base.html.twig' %}

{% block main %}
<section class="flex flex-column pt-15">
    <a class="btn" href="{{ path('admin_recipe_index') }}">Retour à la liste</a>
</section>

<section class="section-content flex flex-column pt-15">
    <h1>Ajouter une recette</h1>
</section>
{% endblock %}